using UnityEngine;

namespace SuperInseto
{
    // Place on a static wall collider or its parent. Disabled markers reject attachment.
    [DisallowMultipleComponent]
    public sealed class ClimbableSurface : MonoBehaviour { }
}
